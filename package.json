{
  "name": "roles-and-permissions-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon app.js",
    "create-migration": "npx sequelize-cli migration:generate --name",
    "create-model": "npx sequelize-cli model:generate --name  --attibutes",
    "migrations": "npx sequelize-cli db:migrate",
    "migrations-undo": "npx sequelize-cli db:migrate:undo",
    "migrations-undo-all": "npx sequelize-cli db:migrate:undo:all",
    "create-seeder": "npx sequelize-cli seed:generate --name",
    "seeders": "npx sequelize-cli db:seed:all",
    "seeders-undo": "npx sequelize-cli db:seed:undo:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^6.14.2",
    "global": "^4.4.0",
    "jsonwebtoken": "^9.0.0",
    "mysql2": "^2.3.3",
    "nodemon": "^2.0.20",
    "sequelize": "^6.28.0"
  },
  "devDependencies": {
    "sequelize-cli": "^6.5.2"
  }
}
